# 整改验收记录（S0–S11）

- 日期：2026-09-11。
- 范围：依据 [代码审查](2026-09-11-code-review.md) 的 S0–S11 整改计划的实际执行结果。
- **本文区分"已通过自动化测试 / 已通过本机实测 / 仍待实测"。编译通过、单元测试通过、单次启动成功都不是整体验收。** 基线提交：`6c6ea52`。

## 1. 整改阶段执行结果

| 阶段 | 状态 | 关键证据 |
| --- | --- | --- |
| S0 数据保护/基线 | 已实施 | git 仓库建立；history 清理止损为保留 7 天原始样本；MONITOR_DATA_DIR 隔离；README 状态校正 |
| S1 测试/契约 | 已实施 | parse/query 模块单测；指标查询校验；typecheck 脚本 |
| S2 采集准确性 | 已实施 | 内存多词字段、GPU 精确键、磁盘显式设备、SMART 范围校验、探测脚本排序/硬编码/重复 ID 修复 |
| S3 独立调度 | 已实施 | Rust 调度器持续采样；IPC 只读缓存；进程独立 System；前后台节奏切换 |
| S4 7 天历史 | 已实施 | 事务内聚合后再删除；raw→10s→60s 幂等聚合；分档查询 |
| S5 存储拓扑 | 已实施 | 物理盘/容器/卷三层模型；共享容量按容器计一次 |
| S6 进程 I/O | 已实施 | proc_pid_rusage 差分；全体进程排序/搜索/分页；PID 重用分离 |
| S7 图表/UI | 已实施 | 真实时间轴、缺口分段、按指标轴、单点不铺面、磁盘选盘 |
| S8 设置/登录项 | 已实施 | 版本化原子保存设置；登录项 osascript 普通权限；可调采样间隔 |
| S9 条件化增强 | 已实施 | 磁盘温度入历史；CPU/GPU 温度保持未实现（不自动提权） |
| S10 资源/安全 | 部分 | CSP 启用；MSRV 修正为 1.88；零警告。长期资源基线未实测 |
| S11 发布链 | 部分 | ad-hoc 严格签名通过；DMG 校验通过；隔离启动验收通过。Developer ID/公证未做 |

## 2. 已通过本机实测（M4 验收证据）

以下在 2026-09-11 用隔离数据目录（MONITOR_DATA_DIR=/tmp，未触碰真实用户数据）实测：

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| 应用脱离开发命令启动 | 通过 | release app 二进制直接启动并运行 |
| 持续采集（非一次性） | 通过 | cpu.total_usage 13 秒内 5 个样本，时间戳递增 |
| 双 SSD 均采集 | 通过 | disk.throughput 含 disk0 与 disk4 |
| 磁盘温度读取 | 通过 | disk.temperature 写入 2 块盘 |
| 逐核 CPU | 通过 | cpu.per_core 写入 10 个核心 |
| 签名完整性 | 通过 | `codesign --verify --deep --strict` 对 DMG 内 app 退出码 0 |
| DMG 完整性 | 通过 | `hdiutil verify` 校验 VALID |
| 干净退出无残留 | 通过 | 终止后无 monitor 相关进程残留 |
| 单元测试 | 通过 | cargo test 28 passed |
| 关窗后菜单栏恢复 | 通过 | tray 环形图标可见，点击弹出 Show/Quit 菜单，Show 恢复窗口，Quit 干净退出 |
| 睡眠/唤醒基线重建 | 通过 | 进程存活跨睡眠；样本总数跨唤醒增长（18887→18904）；唤醒后采样恢复满速（10 秒内 51 个样本）；睡眠窗口保留真实缺口，无伪造样本（V-13/V-15） |
| 总览存储区（SSD 名称/占用率/温度） | 部分 | 代码经构建与逐任务审查通过；dist 产物确认含存储区组件/温度走势/3 列网格。像素级截图在本无头会话不可用（window server 对该 shell 不可见），每行视觉细节待人工在真机核对 |

## 3. 仍待实测 / 未完成（诚实列出，不标通过）

| 项目 | 状态 | 说明 |
| --- | --- | --- |
| 7 天历史跨重启保留 | 待实测 | 聚合逻辑有单测覆盖，但 7 天真实时间跨度未跑满 |
| 前台 1s/后台 3s 实际节奏 | 待实测 | 调度器已实现，实际采样间隔未用秒表核对 |
| 长期资源占用（CPU/内存基线） | 待实测 | 未做 10 分钟以上全进程树资源测量 |
| 稳定性（2 小时+） | 待实测 | 未做长时间运行 |
| 登录项注册/注销 | 待实测 | 代码实现，未在系统设置中验证注册结果 |
| CPU/GPU 核心温度 | 未实现 | 需 IOKit 私有 API 或 root powermetrics；保持 blocked，不自动提权 |
| Developer ID 签名/公证 | 未做 | 当前为本地 ad-hoc；分发需另行授权账号/证书 |
| Windows 扩展 | 冻结 | D-012，Mac 整体验收通过前不启动 |

## 4. 签名与分发说明

当前产物为**本地 ad-hoc 签名**。`codesign --verify --deep --strict` 通过，但 ad-hoc 不提供开发者身份信任链，Gatekeeper 在首次打开时仍会提示。要广泛分发需 Developer ID + 公证，这涉及账号/证书，须单独授权。

构建可复现：`bash scripts/build-release.sh`。R13 起为原子发布：完整构建 → 修复签名 → 打 DMG 到**staging 名** → 校验 → 由 `scripts/gen-manifest.py` 生成溯源 manifest → 仅在全部通过后才把上版 DMG 改名为 `.prev.dmg` 并将 staging 原子换入最终名。失败构建不会触碰既有最终产物。

manifest 记录 source commit、dirty 标记、app 版本、目标架构、rustc/cargo/node/npm 版本、单元测试计数、签名类型与 SHA-256；**不记录**本机目录、主机名、用户名或任何设备标识（符合隐私扫描约束）。

最终 DMG SHA-256：`f82b65f24b11df2ac2c2174e86c13a25d4b2dfab1478f05cbbc714142f682fd7`（`monitor_0.1.0_arm64.dmg`，source commit `338cfcf`，dirty=false；与 `monitor_0.1.0_arm64.manifest.json` 一致）。已对 DMG 内 `monitor.app` 实测：Mach-O arm64、`codesign --verify --deep --strict` 通过（ad-hoc）、资源齐全。上一版 DMG 保留为 `monitor_0.1.0_arm64.prev.dmg` 供回滚。


### GPU 查询截断修复

全量 IORegistry 输出超过 4 MiB 捕获上限时，PerformanceStatistics 被截断，导致整轮采样失败。改为限定 IOAccelerator 节点查询；本机输出约 72 KB，三个 GPU 必需字段齐全。完整只读采样实测通过；81 项 Rust 回归通过，1 项本机硬件测试默认忽略并已单独执行通过，严格 Clippy 通过。新包构建与严格签名校验通过，manifest 与 DMG 哈希一致；本次未声称打包窗口端到端实测。


### 温度曲线假断点修复

对 IPC 返回的历史点先按真实时间缺口分段，再分别降采样；读数选择使用未经过前端降采样的记录。连续平温 1801 点仅绘制一条路径，含一个真实缺口的数据绘制两段；浏览器验证键盘/悬停读数、缺口不显示读数，控制台无错误。临时合成测试页面已删除，不包含在正式产物中。Node 13 项测试通过，修复版构建和严格 ad-hoc 签名检查通过。未启动或停止用户正在运行的实例，未修改历史数据。
